server:
  port: 6061
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cartdb
    username: root
    password: root
  application:
    name: service
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

